Baking — global (Cursor + Claude Code)
========================================

**No per-project install.** Everything is global.

CONFIG
------
  ~/Desktop/side/baking/config.json
    enabled: true
    profile: cursor | hybrid | claude
    handoffDir: .cursor/handoff

CURSOR
------
  ~/.cursor/skills/baking/SKILL.md
  ~/.cursor/agents/baking.md, planner.md, executor-cursor.md
  /baking  or  "use baking for ..."

CLAUDE CODE
-----------
  ~/.claude/skills/baking/SKILL.md
  ~/.claude/agents/baking.md, planner.md, executor.md, executor-mecanic.md

  Update globals:
    & "$env:USERPROFILE\Desktop\side\baking\claude-code\install-claude-skills.ps1"

  Set profile claude in global config for Opus+Sonnet.

HANDOFF (only per-repo artifact)
--------------------------------
  .cursor/handoff/YYYY-MM-DD-slug.md
  Baking creates the folder if missing — do not commit baking config to the repo.

DEPRECATED
----------
  /init-baking, enable-project.ps1, .cursor/opus-sonnet.json, .claude/planner-executor.json

Reference: ~/Desktop/side/baking/BAKING-CURSOR.md (Cursor)
            ~/Desktop/side/baking/claude-code/BAKING.md (Claude Code)
