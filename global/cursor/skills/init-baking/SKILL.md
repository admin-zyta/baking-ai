---
name: init-baking
description: Alias of init-memory — bootstrap AGENTS + Baking Memory. Use /init-memory or baking init-memory.
disable-model-invocation: false
user-invocable: true
---

# init-baking → use **init-memory**

Per-project init is now **`/init-memory`** (Baking 1.6+).

```bash
baking init-memory
```

See the **`init-memory`** skill and `~/Desktop/side/baking/INIT-MEMORY.md`.

Global Baking config is still at `~/Desktop/side/baking/config.json`. Auto-route toggle: `baking auto-route on`.
